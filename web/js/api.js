export class APIError extends Error{constructor(status,message){super(message);this.status=status}}
function cookie(name){return globalThis.document?.cookie?.split('; ').find(value=>value.startsWith(`${name}=`))?.slice(name.length+1)}
export async function request(path,options={}){const headers={Accept:'application/json',...(options.headers||{})};const method=options.method||'GET';let body=options.body;if(body!==undefined){headers['Content-Type']='application/json';if(typeof body!=='string')body=JSON.stringify(body)}if(!['GET','HEAD'].includes(method)){const csrf=cookie('pa_csrf');if(csrf)headers['X-CSRF-Token']=decodeURIComponent(csrf)}const response=await fetch(path,{...options,body,method,headers});const text=response.status===204?'':await response.text();if(!response.ok){let message=text.trim();try{const data=JSON.parse(text);message=data.message||data.code||(data.error?String(data.error).replaceAll('_',' '):message)}catch{}throw new APIError(response.status,message||`Request failed (${response.status})`)}return text.trim()?JSON.parse(text):null}
export function get(path){return request(path)}
export function mutate(path,method,body){return request(path,{method,body:JSON.stringify(body)})}
export function api(path,options={}){return request(`/api/v1${path}`,options)}
